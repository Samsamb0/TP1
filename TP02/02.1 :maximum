	let a;
	let b;
	let c;
	a = Number(prompt("donnes le chiffre"));	
	b = Number(prompt("donnes le second chiffre"));
	c = Number(prompt(donnes le troisième"));
	if (a>b && a>c) {
	
	else if (b>a && b>c) {
	
	else if (c>a &&c>b) {
	
	
  
