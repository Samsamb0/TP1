function executerTp03Ex01_9a() {
    let a = Math.floor(Math.random() * 100)
    let demande= +prompt("veuillez introduire un nombre entre 0 et 100")
    while (demande <a) {
            demande= +prompt("trop petit")
        }
    
   while (demande > a) {
            demande = + prompt("trop grand")
        }
   console.log(a + " chiffre trouvé")
console.log(demande)
    
    }

//solution
function executerTp03Ex01_9a() {
    let a = Math.floor(Math.random() * 100)
    let demande= +prompt("10 chances pour trouver un nombre entre 0 et 100")
    for (demande; demande<a;) {
        demande = + prompt("trop petit")
    for (demande; demande > a;) {
            demande = + prompt("trop grand")
        }
  

    }
 console.log(a + " chiffre trouvé")
console.log(demande)
}
//VRAI SOLUTION
function executerTp03Ex01_9a() {
    let a = Math.floor(Math.random() * 100)
    let demande= +prompt("veuillez introduire un nombre entre 0 et 100")
    while (demande !=a) {
        while (demande <a) {
            demande= +prompt("trop petit")
        }
        while (demande > a) {
            demande = + prompt("trop grand")
        }
    }
    console.log(a + " chiffre trouvé")
    console.log(demande)
}
